# UI Inventory

Checked rows have reproducible automated evidence. Add a new row for a new major surface and link incomplete rows to the current issue register.

| Status | ID | Surface | Expected behavior | Evidence |
| --- | --- | --- | --- | --- |
| [x] | SHELL-001 | Main shell and sidebar | Navigation, window controls, overlays, safe per-thread deletion, About access, tray behavior, and responsive shell remain usable; bulk Clear History is not exposed. Window controls are platform-specific: Windows/Linux draw the in-page minimize/restore/maximize/close strip, while macOS shows no such strip and relies on native traffic lights, whose maximize/restore/fullscreen changes still reach the renderer through `window:state-changed`; top-row chrome reserves title-bar space on whichever side the platform's controls occupy. The tray icon is platform-specific too: Windows and Linux open the window on a left click and keep the menu on the right button, while a macOS status item gives its single click to the menu, so the window opens from Open Jasmine there. | `tests/e2e/shell.spec.ts`, `tests/e2e/threads.spec.ts` |
| [x] | CHAT-001 | Composer and active chat | Rich editing, commands, attachments, streaming, queueing, stopping, editing, and regeneration work; context usage matches Pi's JSONL usage, trailing-message estimate, and post-compaction unknown state. The permission selector persists Ask/Full access, and approval dialogs cannot be dismissed, inherited across a reload, or answered by another renderer. | `tests/e2e/composer.spec.ts`, `tests/e2e/chat-runtime.spec.ts`, `tests/e2e/permissions.spec.ts`, `tests/unit/pi-context-usage.mjs` |
| [x] | THREAD-001 | Threads and projects | Threads, drafts, project scope, paging, search, and deletion persist correctly. | `tests/e2e/threads.spec.ts` |
| [x] | PROVIDER-001 | Providers and models | Provider setup, discovery, testing, selection, model labels, and options expose clear state. | `tests/e2e/providers.spec.ts` |
| [x] | SETTINGS-001 | Settings | General, appearance, brand, language, provider, integration, and window-state controls save visibly; unreleased Chrome Control stays hidden; About identifies Jasmine as the independent desktop app for Pi and shows version/update state, offering the download page instead of in-place install on builds that cannot replace themselves. | `tests/e2e/settings.spec.ts`, `tests/e2e/updater.spec.ts` |
| [x] | PANEL-001 | Work panels | Memory, activity, terminal, artifacts, context, and search panels retain correct ownership and state. Artifacts distinguish added/modified/deleted paths, display the configured managed-tool or watcher coverage semantics, retain failed-run evidence, and lazily show unified text diffs or bounded before/after images when available. The default managed-tool mode never scans a project and explicitly says Shell changes are not tracked; watcher-only evidence can be after-only/path-only and is labeled approximate. Files selected by sensitive-path/content rules keep visible path and metadata while preview bytes and diffs are redacted. Context Taxonomy refreshes as soon as the completed request is persisted, shows every request in the latest user task, groups derived data as messages/tools/at most one request-options item/at most one unclassified-fields item, preserves source-relative order and exact JSONPaths within each group, shows exact raw top-level order separately, auto-expands inner parts with their message, reports reasoning-policy validation, and loads sanitized raw JSON lazily. | `tests/e2e/panels.spec.ts`, `tests/e2e/settings.spec.ts`, `tests/e2e/working.spec.ts`, `tests/unit/pi-runtime-equivalence.mjs`, `tests/unit/database-smoke.mjs`, `src/main/agent/extensions/fileChanges/tests/file-changes.test.mjs` |
| [x] | INTEGRATION-001 | Integrations | MCP, remote SSH, skills, prompt templates, and Pi packages are manageable without exposing secrets; the retired built-in Chrome package is removed without deleting unrelated user packages. | `tests/e2e/integrations.spec.ts` |
| [x] | RENDER-001 | Message rendering | Markdown, code, native thinking, DeepSeek tool preambles, tools, images, traces, and actions are readable and accessible; live agent work stays visible while settled work collapses into a duration recap with the final answer left visible. | `tests/e2e/rendering.spec.ts`, `tests/e2e/chat-runtime.spec.ts` |
| [x] | SPOTLIGHT-001 | Spotlight | Global launcher focus, search, commands, routing, hiding, and restore behavior remain reliable. | `tests/e2e/spotlight.spec.ts` |
| [x] | WORKING-001 | Working task center | The sidebar badge and attention state lead to a global three-group task inbox with authoritative run status, elapsed time, queue count, stop, unread, clear, and exact-chat navigation across projects; task text and controls remain contained by each card. Task notification mode and privacy details save in General settings, and a selected Chat still notifies with an unread fallback after the window moves to the background. | `tests/e2e/working.spec.ts`, `tests/unit/working-registry.mjs`, `tests/unit/database-smoke.mjs` |
| [x] | STARTUP-001 | App startup and repeat launches | Cold start shows startup feedback before initialization, hydrates persisted configuration before the first shell frame, upgrades legacy tables, and folds a repeat launch into the running instance instead of opening a second window. Coverage gap: when a repeat launch races a still-booting instance, the handoff must stay silent rather than raise a main-process error dialog; `main.ts` guards the `will-quit` and `second-instance` handlers against running before app-ready, but that race has no automated regression and is verified manually. | `tests/e2e/startup.spec.ts` |
| [x] | VISUAL-001 | UI audit and visual matrix | Core surfaces expose structured controls without audit errors and can generate ignored visual evidence. | `npm run harness:inspect`, `npm run harness:visual` |
