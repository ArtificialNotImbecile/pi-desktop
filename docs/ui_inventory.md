# UI Inventory

Remote prompt drafts are restored only when submission fails before the remote
process accepts them. A later session/transcript sync failure leaves the prompt
accepted, so the composer never suggests submitting the same turn twice.

Checked rows have reproducible automated evidence. Add a new row for a new major surface and link incomplete rows to the current issue register.

Cross-cutting language contract: static accessible copy on chat-panel
shortcuts, tabs, controls, tooltips, and fields follows the selected interface
language; renderer-owned dates, times, and numeric counts use that language's
locale. The English dictionary preserves the accessible names used by
cross-process E2E selectors. `tests/unit/i18n-parity.mjs` rejects literal
accessible copy and OS-locale formatting in renderer components, while
`tests/e2e/settings.spec.ts` verifies the live Chinese panel wiring.

| Status | ID | Surface | Expected behavior | Evidence |
| --- | --- | --- | --- | --- |
| [x] | SHELL-001 | Main shell and sidebar | Navigation, window controls, overlays, safe per-thread deletion, About access, tray behavior, and responsive shell remain usable; bulk Clear History is not exposed. The application menu carries the standard editing roles (undo, redo, cut, copy, paste, select all) and, on macOS, a real app menu in the first slot: setting an application menu replaces the platform default, and macOS delivers the clipboard shortcuts through those roles, so without them Cmd+X/C/V/A are inert in every input and no paste event reaches the renderer at all. Window controls are platform-specific: Windows/Linux draw the in-page minimize/restore/maximize/close strip, while macOS shows no such strip and relies on native traffic lights, whose maximize/restore/fullscreen changes still reach the renderer through `window:state-changed`; top-row chrome reserves title-bar space on whichever side the platform's controls occupy. Hiding the sidebar keeps its top-row trio — Show sidebar, Search, New chat — as a floating toolbar on the title-bar midline, clear of the platform's left reserve, and every drag strip is emitted before the controls it overlaps so the native draggable region cannot swallow their clicks. The tray icon is platform-specific too: Windows and Linux open the window on a left click and keep the menu on the right button, while a macOS status item gives its single click to the menu, so the window opens from Open Jasmine there. | `tests/e2e/shell.spec.ts`, `tests/e2e/threads.spec.ts` |
| [x] | CHAT-001 | Composer and active chat | Rich editing, commands, attachments, streaming, queueing, stopping, editing, and regeneration work; plain-text paste follows the Lexical cursor or selection while preserving trailing blank lines and multiline clipboard content, and `$`/`@`/`/` command replacement uses the same structural text offset rather than DOM range text that omits line breaks. Cumulative provider output publishes at most once per animation frame and converges to the newest snapshot instead of replaying stale prefixes. Streaming follows the tail until the user explicitly scrolls up, preserves that reading position and any opened thought/tool row through settlement, and returns to the final answer when the reader presses End or reaches the bottom. A stable run header spans first-token wait, tool execution, and text output, standing in at the same place in the stack until the run's own assistant block takes it over on the first frame. Context usage matches Pi's JSONL usage, trailing-message estimate, and post-compaction unknown state. The permission selector persists Ask/Full access, and approval dialogs cannot be dismissed, inherited across a reload, or answered by another renderer. | `tests/renderer/streamTransitions.test.ts`, `tests/e2e/composer.spec.ts`, `tests/e2e/chat-runtime.spec.ts`, `tests/e2e/rendering.spec.ts`, `tests/e2e/permissions.spec.ts`, `tests/unit/pi-context-usage.mjs` |
| [x] | THREAD-001 | Threads and projects | Threads, drafts, project scope, paging, search, and deletion persist correctly. A first message immediately supplies a Unicode-safe fallback title capped at 48 characters; background automatic naming uses a 60-second end-to-end request budget, accepts only a structured, concise, non-conversational title with a normal stop reason, retries one transient request or invalid result, and otherwise keeps that bounded fallback. | `tests/e2e/threads.spec.ts`, `tests/unit/pi-runtime-equivalence.mjs` |
| [x] | PROVIDER-001 | Providers and models | Provider setup, discovery, testing, selection, model labels, and options expose clear state. | `tests/e2e/providers.spec.ts` |
| [x] | SETTINGS-001 | Settings | General, appearance, brand, language, provider, integration, and window-state controls save visibly. General includes an accessible recorder for the global quick-launcher shortcut, reports whether the persisted accelerator is active, applies a free replacement immediately, and keeps the previous binding when the requested combination is unavailable. About identifies Jasmine as the independent desktop app for Pi and shows version/update state, offering the download page instead of in-place install on builds that cannot replace themselves, and offering it as the only action on packaged builds carrying no update feed at all. A development build, which has no installer behind it, still keeps the disabled check button and its reason. | `tests/renderer/providerSettings.test.tsx`, `tests/e2e/spotlight.spec.ts`, `tests/e2e/settings.spec.ts`, `tests/e2e/updater.spec.ts`, `tests/renderer/updaterPage.test.tsx` |
| [x] | PANEL-001 | Work panels | Memory, activity, terminal, artifacts, context, and search panels retain correct ownership and state. Artifacts distinguish added/modified/deleted paths, display the configured managed-tool or watcher coverage semantics, retain failed-run evidence, and lazily show unified text diffs or bounded before/after images when available. Each file occupies one row that states its name once, its directory once, and its added/removed line counts (byte weight when no diff was stored); captures group under a collapsible relative timestamp; semantics that hold for every capture are stated once at the panel level, and a capture only shows a coverage chip and note when that run's evidence is partial, failed, unsupported, shell-untracked, or carried warnings. The default managed-tool mode never scans a project and explicitly says Shell changes are not tracked; watcher-only evidence can be after-only/path-only and is labeled approximate. Files selected by sensitive-path/content rules keep visible path and metadata while preview bytes and diffs are redacted. Context Taxonomy is an opt-in debug probe: only a visible active Context panel enables capture, each thread retains one rolling latest provider request, and requests made while the panel is closed or collapsed perform no taxonomy classification or storage. The live enablement follows panel visibility during a run, so a later queued provider request uses the state when that request starts rather than the initial Send state. The panel refreshes as soon as that request is persisted and reads as three zones: an identity header carrying provider/model, schema, and payload hash; one budget card that owns every diagnostic, stating actual input tokens against the estimate, a composition bar whose legend filters the tree, and cache/reasoning/unknown-field/reconstruction status as chips that expand their own detail in place; and a content tree grouped as instructions/conversation/current prompt/tools/request options/unknown fields. Derived data still preserves source-relative order and exact JSONPaths within each group, and shows exact raw top-level order with the lazily loaded sanitized raw JSON. Composition credits tool definitions and request options to themselves rather than to the `metadata` parts that describe them; message envelope fields (role, tool name, tool call id) are folded into the item meta line while still counting toward its total. Depth reads group → item → part → body and only the leaf body is boxed; kind is carried by one shared colour across bar, legend, item rail and part tag rather than by per-item pills. Only the current user prompt expands by default, plus any reasoning a failed retention check is about; closed items and parts do not mount their Markdown/code bodies, while a filter and Expand all reach the rest on demand. | `tests/renderer/contextTaxonomyPanel.test.tsx`, `tests/e2e/panels.spec.ts`, `tests/e2e/settings.spec.ts`, `tests/e2e/working.spec.ts`, `tests/unit/pi-runtime-equivalence.mjs`, `tests/unit/database-smoke.mjs`, `src/main/agent/extensions/fileChanges/tests/file-changes.test.mjs` |
| [x] | INTEGRATION-001 | Integrations | Skills, prompt templates, and Pi packages are manageable without exposing secrets; the retired built-in Chrome package is removed without deleting unrelated user packages. | `tests/e2e/integrations.spec.ts` |
| [x] | RENDER-001 | Message rendering | Markdown, code, native thinking, DeepSeek tool preambles, tools, images, traces, and actions are readable and accessible. Long cumulative output freezes completed Markdown blocks and updates only the active tail; settled history and jump targets keep stable render identities, and the live message becomes its persisted row without remounting. Every activity row — thinking, tool preamble, tool call, compaction, branch summary — draws one 24px single-line grammar: a 16px leading slot holding the row icon at rest and the disclosure chevron on hover or while open, then the title at 14/24, a 2px separator, and a fill-and-ellipsize summary; nothing sits at the row's end, so all rows share both edges. Thinking summarizes to its latest line only for the active timeline segment and its first line for earlier or settled thoughts, exposed as the toggle's accessible description while the accessible name stays the row's kind so a streaming summary does not re-announce the control. Tool rows name the tool verbatim (Bash, Read, Write, Edit, Search, Fetch) and summarize what it acted on; success carries no status word or byte total at all, and only an edit/write mutation magnitude earns a `flex:none` suffix outside the summary's ellipsis. Run state is carried by a sweeping glare on each row still running — parallel tool calls each keep their own, since they are genuinely running at once — and never on the run header, a red or amber leading dot, and a failed row's first error line replacing its target — each non-nominal state also emitting visually hidden state text, since the colour and motion are aria-hidden. Credential-bearing thought summaries, commands, search queries, and used-memory contents stay behind deliberate disclosure; opening memory provenance during a later run locks auto-follow like every other work disclosure. Authenticated URLs in tool rows and web provenance expose only a sanitized origin/path, credential markers and common standalone provider-token shapes are omitted from every collapsed preview, and a long safe target ellipsizes inside its row rather than widening it. Expanded tool details mount lazily into a single IN/OUT card with sticky gutter labels, a hairline divider, and independently scrolling 150px sections, headed once by the gutter label rather than a duplicated code-block caption. A run header sits at the top of every turn: live it reads Working with the run's model and an m:ss clock, names that model in its accessible name, and carries no glare of its own, leaving the ticking clock as its liveness signal so motion stays on the rows actually running, and on settlement it becomes Worked for/Stopped after/Failed after in place, folding a successful run's activity behind itself while the final answer stays. A stopped or failed run stays open, and a turn with no activity that finished under three seconds carries no header at all. The run header, every row's leading icon, the answer, and the message action icons share one left content edge on the single `--chat-col` axis, and expanded bodies indent from it by exactly 22px. Latin UI text uses the bundled Inter variable font through the private `Jasmine Inter` family at real weights from 100–900 on every platform, while CJK remains on the platform fallback stack. References an answer writes as ordinary Markdown carry real behaviour, and the system prompt asks for exactly that form: a Markdown image with an absolute path is displayed inline, bounded to 320px tall and opening a lightbox on click; a Markdown link to an absolute path becomes a chip badged with the file's extension and tinted by family (document, spreadsheet, presentation, code, data, folder, other), opening the path in the system default application on click and revealing it in the file manager on secondary click; an optional `:line` suffix is shown on the chip but never included in the opened path; and a web link is handed to the OS browser rather than navigating or opening any window. Windows drive paths survive the Markdown sanitizer that would otherwise read `C:` as a scheme, and percent-encoded destinations are decoded before use. A path that does not exist, is not a decodable image, or is too large to paint degrades to a chip — struck through and inert when missing — rather than a broken image; the chunk of a streaming answer still being written holds off resolving its half-typed paths, while each later Markdown message revalidates paths that may have been created or replaced. Credential-bearing web destinations expose only a sanitized URL in link chrome, and every link, file, and image control retains its own label in its accessible name. Remote image sources are never fetched, destinations outside http/https/mailto lose their affordance entirely, and every window denies model-authored navigation. | `tests/renderer/messageRendering.test.tsx`, `tests/renderer/messageLinks.test.ts`, `tests/renderer/messageReferences.test.tsx`, `tests/renderer/safeDisplay.test.ts`, `tests/e2e/rendering.spec.ts`, `tests/e2e/streaming-markdown.spec.ts`, `tests/e2e/message-jump-rail.spec.ts`, `tests/e2e/chat-runtime.spec.ts`, `tests/e2e/panels.spec.ts`, `tests/unit/renderer-font-assets.mjs`, `tests/unit/local-files.mjs` |
| [x] | RENDER-002 | System metadata disclosure | Provider/custom system metadata keeps arbitrary text behind its own lazy disclosure even when a run stopped or failed; the nominal inline Stopped status is credential-sanitized. A final assistant answer remains visible when system metadata follows it. | `tests/renderer/messageRendering.test.tsx`, `tests/renderer/safeDisplay.test.ts` |
| [x] | RENDER-003 | Reference interaction safety | A Markdown image anywhere inside a plain, mixed, or formatted link label is flattened with its alt text into one control owned by the outer destination, never nested links or buttons. Credential-bearing local path segments stay out of tooltips and directory chrome, and GFM autolinks replace destination-derived raw text with the same sanitized URL as their link chrome; the raw target remains available only to the requested action. If opening or revealing a local path, or handing a web link to the browser, fails, the reference shows translated actionable feedback without exposing the OS error. | `tests/renderer/messageReferences.test.tsx` |
| [x] | SPOTLIGHT-001 | Spotlight | Global launcher focus, search, commands, routing, hiding, and restore behavior remain reliable. Its persisted global shortcut defaults to Ctrl+Shift+Space on Windows/Linux and Cmd+Shift+Space on macOS, avoiding Windows' native Alt+Space window menu and the usual macOS Spotlight/Raycast combinations; changing it re-registers without a restart and survives relaunch. | `tests/e2e/spotlight.spec.ts`, `tests/unit/spotlight-shortcut.mjs`, `tests/unit/database-smoke.mjs` |
| [x] | WORKING-001 | Working task center | The sidebar badge and attention state lead to a global task inbox with authoritative run status, elapsed time, queue count, stop, unread, clear, and exact-chat navigation across projects; task text and controls remain contained by each row. The header states the live running and waiting counts, three summary counts filter the list to needs-you/running/done and restore it when pressed again, a group with no tasks renders nothing rather than a placeholder, waiting-for-you reads as attention rather than failure, and a task states its status once: running work shows its current activity and a live elapsed timer, finished work shows how long it took. Starting a chat from the page leaves the Working route instead of stranding the user on an unchanged screen. Task notification mode and privacy details save in General settings, and a selected Chat still notifies with an unread fallback after the window moves to the background. Every line Jasmine writes for itself reads in the selected language, including the activity on a row and the OS notification main sends while the window is hidden. | `tests/e2e/working.spec.ts`, `tests/unit/working-registry.mjs`, `tests/unit/database-smoke.mjs` |
| [x] | STARTUP-001 | App startup and repeat launches | Cold start shows startup feedback before initialization, hydrates persisted configuration before the first shell frame, upgrades legacy tables, and folds a repeat launch into the running instance instead of opening a second window. Coverage gap: when a repeat launch races a still-booting instance, the handoff must stay silent rather than raise a main-process error dialog; `main.ts` guards the `will-quit` and `second-instance` handlers against running before app-ready, but that race has no automated regression and is verified manually. | `tests/e2e/startup.spec.ts` |
| [x] | REMOTE-001 | Remote profiles, interactive sessions, and history | The sidebar carries a Remotes section that groups profiles under their SSH host and states each profile's egress mode, because the same machine with and without the client proxy owns two isolated remote directories and two separate session histories. A status dot is always paired with a status word, and losing the connection reads as remote work continuing rather than as failure. A profile is created through a three-step wizard whose final step is the connection check and runtime install, and whose egress choice is stated as permanent before it is committed. Settings carries the profile detail: host, port, default directory, the doctor report, discovered and manual workspaces, the client-proxy allow list, runtime version, stop, and a removal that promises only what it does -- local connection details and downloaded copies, never remote sessions, credentials, or the installed runtime. A workspace is added by browsing the remote host over plain SSH, which works before any runtime is installed, and directories already carrying sessions are marked. New session opens a local draft composer immediately; its first prompt atomically creates the durable managed Pi session at that cwd, runs it, reconciles the session row, and renders the returned transcript. Running feedback and an explicit Stop action make that lifecycle visible without manufacturing empty remote sessions that Pi does not persist. Session rows state what opening them costs: not downloaded, local copy, updated remotely, or removed remotely. Browsing never installs the managed runtime -- a host without one reports that it needs setup and keeps the rows already stored, leaving the upload to an explicit action -- and an open that fails reaches a retryable failure state rather than a loading line that never resolves. Opening a workspace renders stored rows with no network at all and reconciles behind them; session reconciliation has its own spinner and does not replace Connected with Checking, which is reserved for an explicit connection operation. A session already cached opens offline, one the host has grown fetches only the bytes past the local copy and marks the rows that arrived, one whose remote header was rewritten is downloaded again from the start rather than spliced, and one the host no longer reports stays readable as read-only. Every visible string is translated in both languages, and rendered clocks follow the selected language rather than the machine's locale. | `tests/renderer/remoteSessions.test.tsx`, `tests/unit/remote-sessions.mjs`, `tests/e2e/settings.spec.ts`, `src/main/agent/extensions/piRemote/tests/rpc-client.test.mjs`, `src/main/agent/extensions/piRemote/tests/sessions-read.test.mjs` |
| [x] | VISUAL-001 | UI audit and visual matrix | Core surfaces expose structured controls without audit errors and can generate ignored visual evidence. | `npm run harness:inspect`, `npm run harness:visual` |
