# Workflow Inventory

| Status | ID | Workflow | Success criteria | Evidence |
| --- | --- | --- | --- | --- |
| [x] | WF-001 | Configure a provider and send the first chat | Provider state persists, a selected model responds, Pi JSONL stores the canonical turn, and SQLite projects it for the UI. | `tests/e2e/providers.spec.ts`, `tests/e2e/chat-runtime.spec.ts` |
| [x] | WF-002 | Continue and manage a conversation | Drafts, paging, rename, search, edit, regenerate, queue, stop, and deletion affect the intended thread and its Pi session only; new-chat transitions gate the composer until the active thread settles so immediate typing cannot be reset under a populated sidebar; retry/edit retain inactive JSONL branches; model-switched history is passed to Pi without app-specific request filtering; context usage follows Pi's full-session usage and compaction rules rather than loaded UI text; canonical JSONL, run-linked duration recaps, content-only DeepSeek tool turns, and per-turn reasoning metadata restore correctly after restart. | `tests/unit/pi-runtime-equivalence.mjs`, `tests/unit/pi-context-usage.mjs`, `tests/e2e/threads.spec.ts`, `tests/e2e/composer.spec.ts`, `tests/e2e/chat-runtime.spec.ts`, `tests/e2e/rendering.spec.ts` |
| [x] | WF-003 | Add files and rich content | Attachments, pasted images, markdown, code, previews, and follow-up turns remain usable. | `tests/e2e/composer.spec.ts`, `tests/e2e/rendering.spec.ts` |
| [x] | WF-004 | Configure application settings | Appearance, language, brand, providers, and window state save visibly and restore after reopen. | `tests/e2e/settings.spec.ts` |
| [x] | WF-005 | Use memory and activity | Explicit memory and activity actions persist, remain inspectable, and expose privacy state. | `tests/e2e/panels.spec.ts` |
| [x] | WF-006 | Manage integrations | MCP servers, remotes, skills, prompt templates, and Pi packages install, toggle, and remove safely. | `tests/e2e/integrations.spec.ts` |
| [x] | WF-007 | Work in a local project | Project selection scopes file search, runtime cwd, terminal, and background completion correctly. | `tests/e2e/threads.spec.ts`, `tests/e2e/panels.spec.ts` |
| [x] | WF-008 | Use Spotlight | The global launcher finds threads and commands, restores the app, routes correctly, and returns focus. | `tests/e2e/spotlight.spec.ts` |
| [x] | WF-009 | Inspect agent execution | Live thinking and tool progress remain visible; completed work, model metadata, traces, and every Context Taxonomy provider request become inspectable in the same turn without leaking secrets. Schema v7 separates messages, tools, at most one request-options item, and at most one unclassified-fields item; each group preserves source-relative order and JSONPaths while payload shape records exact raw top-level order. Message expansion reveals all inner parts, DeepSeek/Kimi reasoning replay is checked against the active Pi context, and sanitized raw payload is compressed and lazy-loaded. | `tests/e2e/rendering.spec.ts`, `tests/e2e/chat-runtime.spec.ts`, `tests/e2e/panels.spec.ts`, `tests/unit/pi-runtime-equivalence.mjs` |
| [x] | WF-010 | Validate a release | Build, unit, UI audit, visual capture, full E2E, and headed acceptance complete with ignored artifacts. | `npm run harness:release` |
| [x] | WF-011 | Import Pi session history | Dry-run previews are non-mutating; write runs back up SQLite, copies the original JSONL byte-for-byte with all branches, projects the active branch and project grouping, extracts image assets, and skips previously imported session UUIDs. | `tests/unit/pi-session-import.mjs`, `docs/pi_session_import.md` |
| [x] | WF-012 | Update the installed app | About reports the running version and checks the public GitHub release feed on demand on every installed platform. Where the installation can replace itself — Windows NSIS, Linux AppImage/deb — it exposes download progress and only restarts into the installer after the update is fully downloaded. Where it cannot, because Squirrel.Mac rejects Jasmine's ad-hoc macOS signature, About reports the new version and opens the download page while both in-place steps refuse; the mode is probed from the running bundle's signature. Installations the platform updater disowns, such as a Linux build started outside its AppImage, report as unsupported instead of stalling a check. Development builds explain why update controls are unavailable. | `tests/unit/app-updater.mjs`, `tests/e2e/updater.spec.ts` |
| [x] | WF-013 | Track and resume background tasks | A main-process registry keeps one Working item per Chat across projects, separates waiting/failed/running/recent states, persists unread results and notification preferences, restores stale runs as interrupted, and treats a selected Chat as viewed only while the main window is foreground. Tray-hidden or minimized completion keeps its unread fallback and routes Working rows or Windows notifications back to the exact accessible Chat with project-removal fallback. | `tests/e2e/working.spec.ts`, `tests/unit/working-registry.mjs`, `tests/unit/database-smoke.mjs` |
| [x] | WF-014 | Approve agent actions | Ask mode permits project-scoped writes, asks before every shell command and every external or unscoped write, and fails closed on denial, cancellation, renderer reload/loss, or cross-window responses. Full access bypasses prompts and persists across restart. | `src/main/agent/extensions/permissionGate/tests/permission-gate.test.mjs`, `tests/e2e/permissions.spec.ts`, `tests/unit/database-smoke.mjs` |
| [x] | WF-015 | Inspect agent file changes | Local tracking is configurable: the default fast mode captures only exact successful `write/edit` targets, while the experimental native watcher observes event paths without an initial workspace crawl. Managed targets can show deterministic net diffs and before/after previews; watcher-only updates may be after-only or path-only and explicitly disclose that Bash attribution is unavailable. Conversation edits do not imply filesystem rollback. Sensitive-path/content rules keep visible path and metadata while redacting preview bytes and diffs. Remote SSH runs report unsupported coverage without presenting local files as remote evidence. | `src/main/agent/extensions/fileChanges/tests/file-changes.test.mjs`, `tests/unit/database-smoke.mjs`, `tests/e2e/panels.spec.ts`, `tests/e2e/settings.spec.ts`, `tests/e2e/working.spec.ts` |
